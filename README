HW3 Nextflow pipeline
Profiles
local: runs on the current machine and uses a local conda environment path passed with --conda_env.
cluster: runs on the current machine and creates a conda environment from envs/hw3.yml.
container: runs all processes in the Docker image from --container.

Docker image

docker login
docker build  -t mashakrasha/hw3-nextflow:10
